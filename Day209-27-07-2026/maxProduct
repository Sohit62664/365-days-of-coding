class Solution {
    public int maxProduct(int[] nums) {
        method 01 
        Arrays.sort(nums);
        int first = nums[nums.length-1]-1 ;
        int second = nums[nums.length-2]-1;
        return first * second ;

        
    }
}

